from django.conf.urls.defaults import *
from django.db import models

from ninetyseven.apps.search.views import *

urlpatterns = patterns('',
  url(
    regex   = r'^$',
    view    = search,
    name    = 'search',
  ),
)