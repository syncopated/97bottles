from django.conf.urls.defaults import *
from django.db import models

from ninetyseven.apps.invites.views import *
from ninetyseven.apps.invites.forms import *

urlpatterns = patterns('',
  url(
    regex = r'^send/$',
    view = send_invite,
    name = 'send_invite',
  ),
)